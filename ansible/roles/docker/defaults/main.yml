---
# Docker role configuration defaults

# Docker packages to install
docker_packages:
  - docker.io
  - docker-compose

# Docker service configuration
docker_service_enabled: true
docker_service_state: started

# User configuration
docker_user: "{{ ansible_user }}"
docker_group: docker
